name: 'superligaen'
version: '1.0.0'
profile: 'superligaen'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  superligaen:
    +post_hook: "{{ cleanup_dbt_backup() }}"
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
