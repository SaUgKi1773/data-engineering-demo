name: Silver transformation

on:
  workflow_dispatch:
    inputs:
      full_load:
        description: "Set to 'true' to fully replace all silver tables from current bronze"
        required: false
        default: "false"
      season:
        description: "Season year for seasonal load (e.g. 2025). Leave empty to use rolling window."
        required: false
        default: ""
      lookback:
        description: "Days to look back for incremental window (default: 5)"
        required: false
        default: "5"
      target_db:
        description: "Target MotherDuck database (superligaen or superligaen_dev). Default: superligaen."
        required: false
        default: ""

jobs:
  transform:
    name: Silver transformation
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: dbt deps
        working-directory: dbt
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
        run: dbt deps

      - name: Run silver (incremental)
        if: ${{ github.event.inputs.full_load != 'true' }}
        working-directory: dbt
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DBT_DATABASE: ${{ github.event.inputs.target_db || 'superligaen' }}
        run: |
          TARGET=$([ "${{ github.event.inputs.target_db || 'superligaen' }}" = "superligaen" ] && echo "prod" || echo "dev")
          dbt run --select 'silver.*' --target $TARGET --vars '{"lookback_days": ${{ github.event.inputs.lookback || '5' }}}'

      - name: Run silver (seasonal load)
        if: ${{ github.event.inputs.full_load == 'true' && github.event.inputs.season != '' }}
        working-directory: dbt
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DBT_DATABASE: ${{ github.event.inputs.target_db || 'superligaen' }}
        run: |
          TARGET=$([ "${{ github.event.inputs.target_db || 'superligaen' }}" = "superligaen" ] && echo "prod" || echo "dev")
          dbt run --select 'silver.*' --target $TARGET --vars '{"season": ${{ github.event.inputs.season }}}'

      - name: Run silver (full refresh)
        if: ${{ github.event.inputs.full_load == 'true' && github.event.inputs.season == '' }}
        working-directory: dbt
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DBT_DATABASE: ${{ github.event.inputs.target_db || 'superligaen' }}
        run: |
          TARGET=$([ "${{ github.event.inputs.target_db || 'superligaen' }}" = "superligaen" ] && echo "prod" || echo "dev")
          dbt run --select 'silver.*' --target $TARGET --full-refresh
